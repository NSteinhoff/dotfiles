CompileFlags:
  Add:
    - -xc
    - -std=c17
    - -pedantic
    - -Wall
    - -Wextra
    - -Wconversion
    - -Wmissing-prototypes
    - -iquote=lib

---

If:
  PathMatch: .*\.h

CompileFlags:
  Add: -xc-header
